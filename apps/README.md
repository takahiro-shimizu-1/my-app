# Apps

Each directory under `apps/` is an independently runnable application workspace.

- `web/`: current Next.js + TypeScript + App Router app

Future apps can use a different architecture as long as each workspace has its own `package.json` and the scripts it needs.
